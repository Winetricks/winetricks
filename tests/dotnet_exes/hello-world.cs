// Hello world

using System;

namespace hello {
    class Program {
        static void Main(string[] args) {
            // FIXME: be nice if we could add dotnet versions (running/compiler target) and cpu
            Console.WriteLine("Hello World!");
        }
    }
}
